# di-provider

A lightweight, isomorphic Dependency Injection (DI) container for Node.js and browser environments.

## Features

- Isomorphic: works in Node.js and modern browsers
- Zero runtime dependencies
- Full TypeScript support with bundled declarations (`.d.ts`)
- Dual ESM and CommonJS exports

## Installation

```bash
npm install di-provider
```

## Development

- `npm run build` - Builds ESM and CJS bundles to `dist/` with bundled TypeScript definitions
- `npm test` - Runs test suite via Vitest
- `npm run test:coverage` - Runs test suite with code coverage
- `npm run lint` - Lints source files via ESLint
- `npm run format` - Formats codebase via Prettier

## License

[MIT](LICENSE)
